AAH, or the Annoyingly Advanced Helper, is a basic cmdline program to help me do basic things I should already know.

For example:

```
~$ aah disk mount
- mount a disk:				mkdir /mnt/disk && mount <device> /mnt/disk
```
